# Example environment variables for Split Bill Firebase integration
# Copy this file to `.env` (or add the variables to your CI) and fill in the values
# Vite requires env vars to be prefixed with VITE_ to be exposed to the client.

# Firebase web app configuration - replace the placeholders with values from
# your Firebase project settings (Project Settings -> General -> Your apps)
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Optional: measurement ID (only if using analytics)
# VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Security note:
# - Never commit your real `.env` with secrets to version control.
# - Use this example file as a template and add the real `.env` to .gitignore.

# Usage:
# 1. Copy this file to `.env` in the project root:
#    cp .env.xexample .env
# 2. Fill in the values from your Firebase project console.
# 3. Restart the dev server if it's running (Vite reads env vars at startup):
#    npm run dev
